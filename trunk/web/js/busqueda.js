/**
 * Busqueda en ajax.
 * @author     Roberto Hernandez De La Luz
 */

/*$(document).ready(function()
{
 
 $('#usuario_direccion_direccion').keyup(function(key)
  {
    if (this.value.length >= 3 || this.value == '')
    {
      $('#loader').show();
      $('#jobs').load(
        $(this).parents('form').attr('action'),
        { query: this.value + '*' },
        function() { $('#loader').hide(); }
      );
    }
  });
});*/


